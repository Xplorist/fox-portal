
// const host = "http://10.244.186.185:8083"
const host = "http://127.0.0.1:8080"
const redirectUri = "http://127.0.0.1:8080/portal";

const config = {
  apiUri: `${host}/portal`,
  redirectUri: redirectUri,
};

export default config;
